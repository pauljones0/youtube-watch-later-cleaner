{
  "name": "youtube-watch-later-cleaner",
  "version": "3.0.1",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  }
}
